╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║     🐳 DOCKER PACKAGE - STRUCTURAL HEALTH MONITORING SYSTEM                  ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

📦 PACKAGE CONTENTS
═══════════════════════════════════════════════════════════════════════════════

Docker Configuration Files:
  ✓ docker-compose.yml           - Service orchestration
  ✓ Dockerfile.backend            - Backend container image
  ✓ Dockerfile.frontend           - Frontend container image
  ✓ .dockerignore                 - Build optimization
  ✓ .env.docker                   - Configuration template

Automation Scripts:
  ✓ docker-start.sh              - One-command deployment
  ✓ docker-stop.sh               - Graceful shutdown
  ✓ Makefile                      - Convenient shortcuts
  ✓ test_docker_config.sh        - Validation script

Documentation:
  ✓ DOCKER_QUICK_START.md        - 2-minute guide
  ✓ DOCKER_DEPLOYMENT_GUIDE.md   - Complete technical guide
  ✓ DOCKER_USER_GUIDE.md         - Non-technical user guide
  ✓ README_DOCKER.md             - Package overview

═══════════════════════════════════════════════════════════════════════════════

🚀 QUICK START
═══════════════════════════════════════════════════════════════════════════════

1. Install Docker:
   https://docs.docker.com/get-docker/

2. Start application:
   ./docker-start.sh

3. Access application:
   Frontend UI:  http://localhost:3000
   Backend API:  http://localhost:8000
   API Docs:     http://localhost:8000/docs

═══════════════════════════════════════════════════════════════════════════════

💡 KEY FEATURES
═══════════════════════════════════════════════════════════════════════════════

✅ One-command deployment
✅ Production-ready configuration
✅ Multi-stage optimized builds
✅ Persistent data storage (Docker volumes)
✅ Health monitoring & auto-restart
✅ Resource limits & security hardening
✅ Non-root user execution
✅ Automatic health checks
✅ Cross-platform compatibility

═══════════════════════════════════════════════════════════════════════════════

📊 SERVICES
═══════════════════════════════════════════════════════════════════════════════

Backend (shm-backend):
  - Port: 8000
  - Technology: FastAPI + Python 3.11
  - Resources: 2 CPUs, 4GB RAM (max)
  - Features: ML models, modal analysis, report generation

Frontend (shm-frontend):
  - Port: 3000
  - Technology: React 18 + Nginx
  - Resources: 0.5 CPU, 512MB RAM (max)
  - Features: Interactive UI, visualizations, file upload

═══════════════════════════════════════════════════════════════════════════════

💾 DATA PERSISTENCE
═══════════════════════════════════════════════════════════════════════════════

Docker Volumes (persist across restarts):
  - shm-uploads     : Uploaded CSV files
  - shm-outputs     : Analysis results & reports
  - shm-ml-models   : Trained machine learning models

Backup command:
  make backup

═══════════════════════════════════════════════════════════════════════════════

🎮 BASIC COMMANDS
═══════════════════════════════════════════════════════════════════════════════

Using Scripts:
  ./docker-start.sh              # Start all services
  ./docker-stop.sh               # Stop all services

Using Make:
  make start                     # Start services
  make stop                      # Stop services
  make logs                      # View logs
  make status                    # Check health
  make backup                    # Backup data
  make test                      # Test deployment

Using Docker Compose:
  docker-compose up -d           # Start in background
  docker-compose down            # Stop services
  docker-compose logs -f         # Follow logs
  docker-compose ps              # Check status
  docker-compose restart         # Restart services

═══════════════════════════════════════════════════════════════════════════════

⚙️ CONFIGURATION
═══════════════════════════════════════════════════════════════════════════════

Environment file: .env (copy from .env.docker)

Key settings:
  BACKEND_PORT=8000              # Backend API port
  FRONTEND_PORT=3000             # Frontend UI port
  LOG_LEVEL=INFO                 # Logging level
  DEFAULT_FS=1000                # Sampling frequency (Hz)
  MAX_FILE_SIZE=52428800         # Max upload size (50MB)

═══════════════════════════════════════════════════════════════════════════════

📋 SYSTEM REQUIREMENTS
═══════════════════════════════════════════════════════════════════════════════

Minimum:
  - CPU: 2 cores
  - RAM: 4GB
  - Disk: 10GB free
  - OS: Linux, macOS, Windows (with WSL2)

Recommended:
  - CPU: 4+ cores
  - RAM: 8GB+
  - Disk: 20GB+ free
  - SSD for better performance

═══════════════════════════════════════════════════════════════════════════════

🔐 SECURITY
═══════════════════════════════════════════════════════════════════════════════

✅ Non-root user (appuser, UID 1000)
✅ Resource limits (CPU & memory)
✅ Network isolation (private Docker network)
✅ Health checks (auto-restart on failure)
✅ Minimal base images (Python slim, Alpine)
✅ Multi-stage builds (reduced attack surface)

═══════════════════════════════════════════════════════════════════════════════

🛠️ TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Common Issues:

1. Docker not running:
   → Start Docker Desktop or: sudo systemctl start docker

2. Port already in use:
   → Change BACKEND_PORT or FRONTEND_PORT in .env

3. Permission denied:
   → sudo usermod -aG docker $USER (then logout/login)

4. Services won't start:
   → Check logs: docker-compose logs backend

5. Out of memory:
   → Increase Docker memory: Docker Desktop → Settings → Resources

═══════════════════════════════════════════════════════════════════════════════

📚 DOCUMENTATION GUIDE
═══════════════════════════════════════════════════════════════════════════════

Choose your guide:

For End Users (Non-Technical):
  → Read: DOCKER_USER_GUIDE.md

For Quick Setup (2 minutes):
  → Read: DOCKER_QUICK_START.md

For Technical Details:
  → Read: DOCKER_DEPLOYMENT_GUIDE.md

For Package Overview:
  → Read: README_DOCKER.md

═══════════════════════════════════════════════════════════════════════════════

🌐 NETWORK ACCESS
═══════════════════════════════════════════════════════════════════════════════

Local access:
  http://localhost:3000

LAN access (from other devices):
  1. Find your IP: ip addr show (Linux) / ipconfig (Windows)
  2. Access: http://YOUR_IP:3000

Internet access:
  ⚠️ Requires HTTPS setup - See DOCKER_DEPLOYMENT_GUIDE.md

═══════════════════════════════════════════════════════════════════════════════

🔄 MAINTENANCE
═══════════════════════════════════════════════════════════════════════════════

Regular tasks:

Weekly:
  docker system prune            # Clean unused resources

Monthly:
  docker-compose pull            # Update images
  docker-compose up -d           # Restart with updates

As needed:
  make backup                    # Backup data
  docker-compose logs -f         # Monitor logs

═══════════════════════════════════════════════════════════════════════════════

✅ DEPLOYMENT CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

Before production deployment:

[ ] Install Docker (20.10+)
[ ] Configure .env file
[ ] Test: ./docker-start.sh
[ ] Verify: curl http://localhost:8000/health
[ ] Access UI: http://localhost:3000
[ ] Set up backups: make backup
[ ] Configure firewall (if needed)
[ ] Set up HTTPS (for internet access)
[ ] Document access URLs
[ ] Test disaster recovery

═══════════════════════════════════════════════════════════════════════════════

📊 BUILD INFORMATION
═══════════════════════════════════════════════════════════════════════════════

Package Version: 1.0.0
Build Date: 2026-01-29
Docker Version Required: 20.10+
Docker Compose Version: 3.8
Backend Image: Python 3.11-slim (~1.2GB)
Frontend Image: Nginx 1.25-alpine (~50MB)

═══════════════════════════════════════════════════════════════════════════════

🎉 SUCCESS INDICATORS
═══════════════════════════════════════════════════════════════════════════════

Your deployment is successful when:

✅ docker ps shows 2 running containers (shm-backend, shm-frontend)
✅ curl http://localhost:8000/health returns {"status": "healthy"}
✅ http://localhost:3000 displays the UI
✅ Can upload a CSV file and run analysis
✅ Can download PDF/JSON reports

═══════════════════════════════════════════════════════════════════════════════

📞 SUPPORT
═══════════════════════════════════════════════════════════════════════════════

Getting help:
  1. Check logs: docker-compose logs -f
  2. Verify health: curl http://localhost:8000/health
  3. Check status: docker-compose ps
  4. Review documentation in this package
  5. Test deployment: make test

═══════════════════════════════════════════════════════════════════════════════

🚀 READY TO DEPLOY!
═══════════════════════════════════════════════════════════════════════════════

Run: ./docker-start.sh

Access: http://localhost:3000

═══════════════════════════════════════════════════════════════════════════════

╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                    🔧 FIX DOCKER PERMISSIONS - STEP BY STEP                  ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

PROBLEM: Your user can't access Docker without sudo

SOLUTION: Run these commands in your terminal (one at a time)

═══════════════════════════════════════════════════════════════════════════════

STEP 1: Add yourself to docker group
────────────────────────────────────────────────────────────────────────────────

Run this command and enter your sudo password when asked:

    sudo usermod -aG docker $USER

Expected output: (no output means success)

═══════════════════════════════════════════════════════════════════════════════

STEP 2: Activate the group (choose ONE method)
────────────────────────────────────────────────────────────────────────────────

Method A: Use newgrp (no logout needed)
    
    newgrp docker

Method B: Logout and login again (more reliable)
    
    logout
    # Then login again

═══════════════════════════════════════════════════════════════════════════════

STEP 3: Test Docker access
────────────────────────────────────────────────────────────────────────────────

    docker ps

Expected: Should show containers without asking for password

═══════════════════════════════════════════════════════════════════════════════

STEP 4: Start the application
────────────────────────────────────────────────────────────────────────────────

    ./docker-start.sh

Expected: Should start building and deploying

═══════════════════════════════════════════════════════════════════════════════

ALTERNATIVE: Use sudo directly (if above doesn't work)
────────────────────────────────────────────────────────────────────────────────

    sudo docker-compose up -d

This will ask for password but will work immediately.

═══════════════════════════════════════════════════════════════════════════════

QUICK REFERENCE: Commands you need
────────────────────────────────────────────────────────────────────────────────

1. sudo usermod -aG docker $USER
2. newgrp docker
3. docker ps
4. ./docker-start.sh

OR

1. sudo docker-compose up -d

═══════════════════════════════════════════════════════════════════════════════
